## Export

```bash
repograph export
repograph export --output path/to/repograph.md
```

Regenerates **`.repograph/repograph.md`** from SQLite. Use as a **read-only snapshot** for agents; do not treat markdown as canonical.

**`changes finalize --export`** can refresh markdown after finalize.

Details: `repograph export --help`.
